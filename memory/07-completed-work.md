# 07 — Completed Work (what was done & why)

All work below is complete and verified as of 2026-08-20.

## Auth UI split: dedicated /register + polished login (2026-08-24)

Login and register are now two separate pages (no more sign-in/create-account toggle inside `SignInForm`):

- **`/login`** — same KeebForge auth card; `SignInForm` is sign-in only (Google/Discord via shared `OAuthButtons`, email/password, eye toggle, Forgot password?, "Create an account →" link to `/register`, terms footer).
- **`/register`** — server shell + `RegisterForm`: username (helper text + **real** availability check via `GET /api/auth/check-username?u=` against the DB, debounced), email with inline validation, password + confirm each with independent eye toggles, live requirements checklist (`○`→lime `✓`) driven by shared `src/lib/password.ts`, terms checkbox gating the submit button, "Creating account…" loading state, mapped error copy (422 → "account already exists…"), best-effort username claim after successful signup.
- **Username is real**: new `Profile.username String? @unique` (migration `20260824000000_profile_username`), format rule in client-safe `src/lib/username.ts`, claim via existing `PATCH /api/account/profile` (lowercased, uniqueness-enforced).
- **Password policy enforced server-side**: BA 1.7.1 has no `password.validate` option → added an auth before-hook (`createAuthMiddleware`) rejecting weak passwords on `/sign-up/email` + `/change-password`; rules come from the same module the UI checklist renders, so they can't drift.
- **Fixed a real account-API gap**: `/api/account/{profile,addresses}` used a raw `profile.findUnique({userId})`, which 401s right after sign-up (Profile row doesn't exist until `getCurrentAuth()` auto-creates it) — the immediate username PATCH hit exactly that window. All three account route helpers now use `getCurrentAuth()`.
- Verified live (curl e2e, test data cleaned up): all pages 200; weak password rejected by hook; valid signup 200 + session; duplicate email 422; invalid login 401; valid login 200; free/taken/invalid username checks; username claim immediately post-signup; `/api/auth/me` shows plain CUSTOMER (no org/admin assignment). Typecheck/lint/build green. OAuth redirects themselves remain blocked on provider-console callback registration (see roadmap).

### Auth card layout lessons (2026-08-24)

- **Auth routes moved under /auth/**\* (2026-08-24): `/login`→`/auth/login`, `/register`→`/auth/register`, `/forgot-password`→`/auth/forgot-password`; pages live in `src/app/(auth)/auth/`. Old paths 307-redirect via next.config REDIRECTS. All internal links/redirects updated (SiteHeader, AdminShell, account guards, lib/auth, order-success, unauthorized, invite). Auth pages have no footer: route groups `(auth)` (header only) vs `(site)` (header+footer); root layout is just html/body.
- Footer sits below the fold on short pages via `.page-main { min-height: 100vh }` — required because `.nav` is position:fixed/out of flow, so flex-1 alone let the footer start inside the first viewport (measured with Playwright at 1366/1920/390 widths).
- Final auth layout: wide two-column card (`auth-card.auth-card-wide`, max-width 860px, radius 20px) — form left (~60%), dedicated `auth-divider` element + social panel right (~40%); single column below **1000px** (never force tiny columns); Playwright-measured at 1440/1200/1000/830/768/390.
- **Gotcha that bit us twice**: `.auth-card-wide` (one class) vs `.auth-card` (one class) = equal specificity → source order decides → the later base rule silently capped the card at 560px while the override said 860px. Fix: use `.auth-card.auth-card-wide` (two classes) for any property that fights a base `.auth-card` rule. Verify rendered widths with a headless browser, never trust class names.
- Password requirements: clean 2-col grid (`req-title` + `req-list`), no boxed background; social buttons fixed 48px height with nowrap text; inputs ~50px tall.

## Cloudinary image system — audited, fixed, e2e-verified (2026-08-24)

The system was already ~90% built (`Media` model + enums, `/api/uploads` POST/DELETE with `getAdminContext`/`canAction` authz, server-decided folders, magic-byte validation, `cldUrl` f_auto/q_auto delivery, ProductForm upload progress/reorder/delete/draft-rename, RepairImagesPanel per-role sections, repair-request customer uploads with orphan cleanup). This pass closed the remaining gaps and proved it end-to-end against live Cloudinary:

- **Fixed broken admin repair uploads**: `RepairImagesPanel` expects `data.media`, but `/api/uploads` returned flat fields → every admin repair upload showed an error after succeeding. Route now returns the created Media row as `media` (flat fields kept for the product form).
- **Fixed folder keying inconsistency (case bug)**: `repairRoleFolder` compared roles case-sensitively ("BEFORE" vs "before") so ALL repair images — customer uploads included — landed in `…/other/`. Now normalizes; roles map to their sub-folders. Also: admin REPAIR uploads are now keyed by the order's `orderNumber` (`keebforge/repairs/KF1234567/before`), matching the customer flow — previously admin uploads used the OrderRepair cuid.
- **AVIF accepted everywhere** via new client-safe `src/lib/image-validation.ts` (shared MIME set + ISO-BMFF magic-byte sniffer), replacing three copy-pasted sniffers in uploads route / repair-request / inquiry; all four `accept=` attrs updated.
- **Orphan safety**: if a Media-row insert fails after a successful Cloudinary upload, the asset is destroyed before returning the error.
- Verified by scripted e2e (13 checks, live DB + Cloudinary): unauth 401, admin sign-in, PRODUCT draft → `products/drafts/{token}`, REPAIR photo → `repairs/KFE2ETEST/before`, non-image rejected on magic bytes, unknown repair id → 404 (no arbitrary folders), DELETE removes row + asset (verified gone via Cloudinary Admin API, not CDN), favicon local 200. Test data fully cleaned up. Typecheck/lint/build green; no `CLOUDINARY_API_SECRET` in `.next/static`.
- Deliberate ceiling (`ponytail:` note in cloudinary.ts): customer repair/order assets are public-but-unlisted (opaque ids); truly-private delivery would need an auth-gated proxy route. Deferred: service images (Service has no image field — nothing exists to migrate) and a WorkImage model for custom-work (WorkProject.images JSON already accepts Cloudinary URLs).

## Footer rebuilt as full brand footer (2026-08-22)

`Footer.tsx` rewritten on the existing `site-footer` system (same `--max-w`/`--pad-x` container as `.wrap`, watermark + lime top border kept). Five columns: brand (new copy, live `acceptingOrders` status badge — dot turns red via `data-off`, "Follow KeebForge" mini icon row), Shop, Services, Company, Support. Socials centralized in a `SOCIALS` const with the owner's REAL profiles from `/about` (Reddit u/hardy_022, Discord hardy_022, Instagram @nowitshardik; GitHub → github.com/hardy0022; YouTube handle placeholder until live). No icon lib installed → five inline SVG brand marks (24×24 grid). COMMUNITY band above bottom bar with labeled social chips (38px, lime hover). Support column renders Track Order / Shipping / Returns as non-link rows tagged SOON (`footer-link-soon`) — **the old `/track` footer link was a 404** (route doesn't exist). Shop column uses only real category slugs (switches/keycaps/accessories verified 200); `_` prefixes dropped; readability bumped (links .86rem, blurb .88rem, bottom line .72rem). Responsive: ≤1080px 2-col grid (brand spans), ≤768px single column, community stacks. Verified: typecheck/lint/build green; all footer hrefs resolve (0 dead links); all six shop sub-routes 200.

## Homepage marquee rebuilt as seamless ticker + reusable component (2026-08-22)

Hero badge ("India-Wide Mail-In Service") removed per owner. Hero CTAs reworked: primary "Start a Build / Service →" → `/services`, secondary "Shop Keyboards & Parts →" → `/shop` (was Place an Order→/checkout, Explore Services→/services); ghost secondary matched to `btn-prime-lg` height via scoped `.hero-cta .btn-ghost` rule (0.92rem, 13px padding, gap 7px), full-width stack ≤480px. Marquee extracted from `page.tsx` into `src/components/ui/Marquee.tsx` (reusable `<Marquee items={string[]} />`) with owner-supplied content (10 services incl. Split Keyboard Builds, Keyboard Modifications, Keyboard Parts & Accessories). Seamless-loop mechanics: two identical `.marquee-group` halves, all spacing INSIDE groups via `.marquee-star` margins — **never flex `gap` on the track**, it makes `translateX(-50%)` land mid-gap and the loop visibly jumps (the old bug). 36s linear infinite desktop, 26s ≤640px, disabled under `prefers-reduced-motion`; lime ✦ separators, existing tokens only. Verified: typecheck/lint/build green; served HTML shows exactly 2 groups × 10 items; served CSS carries 36s/26s/reduced-motion rules.

## Per-service detail pages removed (2026-08-22) — D-022

`/services/[device]/[slug]` deleted with owner approval; all inbound links repointed to the `/services` configurator (homepage `ServiceSection`, 13 legacy redirects, footer), service URLs dropped from sitemap, dead `ServiceTable.tsx` + `getServiceBySlug()` removed. Verified: typecheck/lint/build green; removed route 404s, legacy redirect 308→`/services`, homepage renders no detail-page links, sitemap clean.

## Product detail page rebuild (2026-08-22)

`/product/[slug]` rebuilt as a proper ecommerce PDP on existing primitives: `<main className="product-page">` now shares the shop's wide container (`--container-w`) + navbar alignment; two-column hero (~55/45, gallery sticky) with kicker row (brand link · category · Custom/Clearance chip), rating row (only when `ratingCount > 0`, via `Stars`), price panel using `formatINR`/`formatINRRange` (**fixes raw-paise display** — old page printed `899900`; JSON-LD was already correct), struck compare-at + `Save N%` badge, "Starting from" prefix for CUSTOM/variant products, type-aware stock line (CUSTOM → "Made to order", OOS disabled state), compact meta tags. `AddToCart` reworked: −/+ qty stepper, full-width `btn-prime btn-prime-lg`, duplicate price/stock text removed (page owns both); server action wiring unchanged. Details band below hero: dynamic specs panel (empty values filtered) | Key Features + What's Included lists; description block max-width; shipping/warranty split; Related uses `/shop`'s exact `ProductCard` + `.shop-grid`. `getRelatedProducts` tops up thin categories with other active products to always fill 4. Image "empty box" report investigated: seeded images all valid local webp files served fine — no bug found; letter-fallback remains for genuinely imageless products. Verified: typecheck/lint/build green; G502 (₹8,999/₹10,999/Save 18%), Keychron, stabilizer pages 200 with 4 related each; no reachable OOS product in seed (cable is active:false by design, D-014) — OOS UI path exists but unexercised; `/shop` untouched.



## /services landing page redesign (2026-08-22)

`/services` is now a marketing landing page; the interactive configurator moved to `/services/configure` (all landing CTAs and popular-service cards link into it or its `[device]/[slug]` detail pages). Landing structure: hero (~68vh desktop, collapsed on mobile) with eyebrow/lime-span heading/dual CTAs and an orb+badge visual (SWITCHES/STABILIZERS/SOLDERING/MOUSE MODS/PCB REPAIR, lime+purple glow); "What we do" 2×2 category cards (Keyboard / Mouse / Repairs / Custom, linking to `/services/configure` ×2 + `/repair` ×2); "Popular services" — 6 curated cards whose prices render live from the DB via a `prisma.service.findMany` slug query (D-005 held: no hardcoded ₹); keyboard split (CSS keycap board + SWITCHES/STABILIZERS/BUILD/MODS highlight rows); mouse split (SVG mouse, reversed); repair panel (dashed border, fault list → `/repair`); custom-work panel (pulsing CPU icon → `/repair`); 3-step "How it works" with outlined numbers; minimal "Why KeebForge" rows; shared `FaqList`; final CTA panel. Styles are the `sv-*` block in `globals.css` (`--container-w`, navbar matched by `body:has(main.sv-land)`, hover lifts only, `prefers-reduced-motion` respected). Also fixed the site-wide footer "Custom Builds" link which 404'd (target service inactive) — now points to `/repair`. Verified: typecheck/lint/build green; `/services`, `/services/configure`, all 6 popular slugs, `/repair`, `/shop`, `/work`, `/cart` → 200.

## /repair rebuilt as Custom Work & Repairs intake (2026-08-22) — supersedes D-019's email-only flow

`/repair` is now a full intake page: compact hero ("CUSTOM WORK & REPAIRS" + capability badges), then `RepairIntake` (`src/components/repair/RepairIntake.tsx`, client) — sections 01 Service (large selectable cards: Custom Work / Repair / Not sure, lime selected state), 02 Device (device pills, brand, model/PCB), 03 Work Required (chips that adapt to the chosen service + condition + optional budget + description), 04 Photos (prominent upload zone, ≤5 images, thumbnails with remove — same validation rules as InquiryForm), 05 Your Details (auto-filled from the signed-in profile, editable), 06 Shipping (ship / pickup / unsure; saved profile addresses offered when shipping, manual entry otherwise). Right column: sticky live Request Summary with the "Final price will be confirmed after inspection" note. Review state shows every entered value with Edit Request / Submit Request; success state shows "Request Received" + reference number + View Request (`/order/success/<orderNumber>`) / Back to Home.

New server action `submitRepairRequest` (`src/app/actions/repair-request.ts`): zod-validated, photos re-validated + magic-byte sniffed → Cloudinary `keebforge/repairs`, then **persists a real Order** (`type: REPAIR`, status ORDER_RECEIVED, guest-capable via snapshots) + OrderRepair + OrderAddress(ship only) + timeline, `syncTrackingCache`, Resend email with all fields + photo links (email failure doesn't fail the persisted request). Reuses `generateOrderNumber()` so View Request works against the existing order pages. Layout ~1160px two-column, stacks on mobile. Verified: typecheck/lint/build green; `/repair` 200 rendering hero + service cards + summary; `/services`, `/`, `/shop`, `/login` unaffected.

## Hero refinement (2026-08-22)

Home `Hero.tsx` refined per owner spec (no redesign): heading → "Precision Keyboard / Builds, Tuning & Repairs" with lime second line, size clamp(2.2,7vw,4.5)→clamp(2rem,5.5vw,3.6rem); description replaced with the custom-builds/tuning/soldering/mouse copy; secondary CTA renamed "Explore Services →"; added muted service-highlight line under CTAs; content nudged up (`pb-[9vh]`) with tightened vertical gaps; background kept but layered scrims added (darker center core + vignette + bottom fade, brightness 0.58) for text contrast while keeping the keyboard visible. Badge kept per spec §4/§8 despite the opening line saying "remove" (structured spec won). Shared components (ServiceSection/FaqList/CtaSection) untouched — they also render on locked `/services`.

## Navbar overhaul — global sizing, left-aligned structure, cart badge (2026-08-22)

Second pass on `SiteHeader` (already global via root layout + `ShowOnSite`; no page-level navbar duplication exists). KeyDir.in-inspired positioning: `.nav-logo` lost its `margin-right:auto`, so brand + links now sit left together with `.nav-actions` (`margin-left:auto`) holding cart/profile right. **Centering root-caused and removed**: `.nav-inner` was a width-constrained block with `margin:0 auto` — a centered strip, so the whole content group started mid-screen on wide viewports; it is now `width:100%; max-width:none; margin:0` with `padding-inline: clamp(20px,4vw,48px)` (full-bleed row, left cluster = logo+links at the viewport edge, right cluster pinned by actions' auto margin; the ≥1024 container-width override was deleted). Content sized up inside the unchanged 60px `--nav-h`: logo 0.97→1.15rem, nav links 0.78→0.9rem w600 padding 8px, action buttons 38→40px, icons/chevron/avatar svg bumped, mobile menu links 1rem. Cart count badge added: fetches the existing `/api/cart` on mount/pathname change, re-synced same-page via a `kf-cart-changed` window event dispatched from `AddToCart` + `CardAddToCart` success effects (no second cart system); badge hidden when empty, "9+" cap, aria-label includes count. No search icon exists anywhere. Verified: typecheck/lint/build green; all key routes 200; served CSS shows no centering on `.nav-inner`; dropdown renders client-side only (not in SSR HTML — expected).

## Navbar polish (2026-08-22)

`SiteHeader` + nav CSS in `globals.css` refined without touching any page layout: active nav item gets a subtle lime underline + `aria-current="page"` (desktop and mobile, `usePathname()`); right-side actions (cart / avatar / sign-in icon / hamburger) grouped into `.nav-actions` sharing one unified button rule (38px, radius 12, same border/hover) — the old duplicate `.nav-icon` (Shop section) and `.nav-profile-trigger` blocks were deleted; sign-in is now an icon button consistent with cart. On configurator pages (`body:has(main.config-page)`, i.e. `/services`) the navbar at ≥1024px matches the page's wide container (`min(1280px, calc(100% - 96px))`) so it aligns with Device Details / Order Preview. Nav bar: `--nav-h` 56→60px (all offsets are token-based), always-on faint bottom border + light blur when unscrolled (intensifies on scroll), link gap clamp(20–32px) with slightly larger type. Mobile menu: active state (lime left inset bar), inline styles replaced by `.mobile-menu-extra`, redundant Cart row dropped (cart icon stays visible in the header). Follow-ups (2026-08-22): `.order-wrap` top margin now `calc(var(--nav-h) + clamp(28px, 4vw, 44px))` (~28–44px gap below nav); a scoped purple-strip on `/services` was applied then reverted same day at the owner's request — the page keeps the original site-wide purple radial background. Verified: typecheck/lint/build green; `/services` renders `nav-link active` + `nav-actions`; `/`, `/shop`, `/repair` still 200.

## Service configurator + admin pricing (2026-08-20)

`/services` is now an interactive configurator matching the old `shop.keebforge.in/order/` page (no order form): device tabs (Keyboard/Mouse with selected counts), component quantity steppers (switches 65 / stabilizers 4 / mouse switch units 2), checkbox service matrix grouped by `ServiceGroup` with live combo/exclusive de-selection (`replaces`/`exclusiveWith` slugs), per-unit price labels (`+₹12/SW`, ranges `₹500–550`, `Quote` badges), and a sticky subtotal estimate (per-switch/per-stab × qty; ranges + quotes listed as "Pending Review"). Data is DB-driven (D-005) — `src/components/services/ServiceConfigurator.tsx` (client) renders from `getServiceCatalog()`.

New `/admin/services` page (sidebar link already existed, previously the stub): a per-service price editor (`ServicePriceForm` + `saveService` action in `src/app/admin/actions/services.ts`) — unit select, price/min/max (rupee input → paise, matching the product-form convention), price label, sort order, combo/popular/highlight/active toggles. Reference CSS ported into `globals.css` (`.panel`, `.device-tabs`, `.service-row`, `.qty-*`, `.total-*`, `.summary-box`). Verified: typecheck/lint/build green; `/services` 200 with full matrix; `/admin/services` 307→`/login` unauthed. Client component count is now 5 (see `02-architecture.md`).

## Homepage restore (2026-08-20) — D-018 superseded

The maintenance page was removed and the full homepage rebuilt (see D-018 in `06-decisions.md`): `Hero`, text marquee strip, DB reviews marquee (`getApprovedReviews()` added to `src/lib/data.ts`, reusing the surviving `.review-card`/`.reviews-*` CSS), all `ServiceSection` groups, `GENERAL_FAQ` via `FaqList`, and `CtaSection`. Fixed the `Hero` "View Services" link which pointed at the non-existent `/services/keyboard` (now `/services`). Verified: typecheck/lint/build green; production server `/` returns 200 with hero + reviews + FAQ rendered.

All earlier work below is complete and verified as of 2026-08-19. Phase descriptions include the "why" — the reasoning that shaped each stage.

## Phase 3 — Foundation
**Why:** before any content, the skeleton had to hold real data and real traffic.

- Next.js 16.3.1 App Router app, TypeScript strict, Tailwind v4.
- "v3 lime" theme: lime-400 accent on near-black, Geist font, tokens in `globals.css`.
- Layout primitives: `SiteHeader` (client, cart badge), `Footer`, UI kit (`Button`, `SectionHead`, `PageHero`, `Checklist`, `FaqList`, `Breadcrumbs`, `Stars`, `ReviewCard`, `PriceDisplay`, `CtaSection`).
- Homepage: hero, service cards, reviews marquee (`ReviewsMarquee`), DB-backed FAQ (`Faq`).
- SEO: `sitemap.ts`, `robots.ts`, metadata; `next.config.ts` redirect map.
- Prisma wired to Supabase PostgreSQL; server-only data fetching with `React.cache()`; `src/proxy.ts` middleware (Supabase session refresh at the time — replaced in Phase "Auth Migration").
- Verified: `prisma validate/generate/status`, `tsc`, lint, build, route smoke tests.

## Phase 4 — All content pages
**Why:** the site had to present the full business before commerce; each page is DB-driven.

- `/services` + `/services/[device]/[slug]` — catalog from `ServiceGroup`/`Service`, JSON-LD.
- `/repair` — Electronics Repair + photo-enabled inquiry form (replaced the per-device `/repair/keyboard` + `/repair/mouse` pricing pages, removed 2026-08-20).
- `/reviews` — approved reviews from DB.
- `/work`, `/work/[slug]` — portfolio from `WorkProject`.
- `/about`, `/faq` (DB FAQs), `/terms`, `/contact` (Resend form — verified 200), `/thanks`, `/not-found`.
- Case-variant redirects (`/About`, `/Review`) moved into `src/proxy.ts`.
- Resolved a transient pooler 500; sitemap verified; `robots.ts` added (disallows admin/account/checkout/api/track).

## Phase 4.5 — Full audit (all PASS, with fixes)
**Why:** a security/pricing/SEO pass was explicitly requested before building commerce.

- **Database/Prisma:** migrations all applied; no Supabase JS CRUD in app code; no Prisma in browser bundles.
- **Money:** integer paise everywhere (D-001).
- **Pricing:** removed hardcoded ₹ from 3 files → DB-driven (D-005). Created `.env.example`.
- **Auth:** no auto-admin (D-002); `requireAdmin`/`requireUser` server-side.
- **Routes:** `/checkout` had been a 404 while 7 places linked it → built a real minimal checkout shell (D-013). Dropped `loading.tsx` soft-404 (D-009).
- Schema enhancement migration `20260819170000_shop_catalog_models` (brand/product fields, reserved stock, cart guest support).

## Phase 5 — Shop & cart
**Why:** the catalog needed a working buy-path foundation before payments (Phase 6).

- Queries in `src/lib/data.ts`: `getShopProducts` (filters/sort/pagination), `getProductBySlug`, `getRelatedProducts`, category/brand lookups.
- Cart in `src/lib/cart.ts`: owner resolution (profile or guest token), items, `availableQuantity`.
- Server actions `src/app/actions/cart.ts`: `addToCart`, `updateCartItem`, `removeCartItem` — zod-validated (qty ≤ 50), server-side stock enforcement (e.g. "Only 8 available").
- Components: `AddToCart`, `ProductCard`, `ShopGrid`, `ShopFilters` (client-free filtering), `CartQty` (client).
- Routes: `/shop`, `/shop/[category]` (404 invalid), `/product/[slug]` (gallery, variant selection, specs, related, JSON-LD), `/cart`, `/checkout`.
- `error.tsx` for shop + product; removed `loading.tsx` (D-009).
- Theme: shop styles + `--err/--ok/--warn` tokens in `globals.css`; cart link in `SiteHeader`.
- Verified: add-to-cart via action-protocol POST (valid add, over-stock "Only 8 available", bad variant "This variant is not available", zod reject); route + redirect smoke tests; empty states; full build.

## Phase "Auth Migration" — Supabase Auth → Better Auth (2026-08-19)
**Why:** single auth authority, self-owned sessions, Dash admin dashboard (see `04-auth.md` for full detail).

- Installed `better-auth` 1.7.1, `@better-auth/infra` 0.4.0, `@better-auth/cli` 1.4.22, `cloudinary` 2.10.1.
- Auth instance in `src/lib/auth/better-auth.ts`; handler at `src/app/api/auth/[...all]/route.ts`.
- Auth tables generated via CLI; `Account.issuer` fixed (CLI bug); migrations `20260819180000_better_auth` + `20260819181000_better_auth_account_issuer` applied.
- `Profile.userId` added (nullable unique) linking business identity to auth identity; admin seed profile unaffected.
- Rewrote `src/lib/auth.ts` (`getCurrentAuth` via `auth.api.getSession`), simplified `src/proxy.ts`, deleted `src/lib/supabase/*`, removed `@supabase/ssr` + `@supabase/supabase-js`.
- `.env`: added `BETTER_AUTH_SECRET` (generated) + `BETTER_AUTH_URL`; `.env.example` updated (auth + Cloudinary placeholders).
- Fixed Turbopack build failure from `@better-auth/infra` dynamic imports (D-011).
- Verified end-to-end: sign-up 200, sign-in 200, wrong password 401, get-session returns user, sign-out → session null; all 20 routes build; all redirects 308; unknown product slug 404.

## Phase 8 — Admin (Part 1): auth, shell, dashboard, orders (2026-08-20)
**Why:** the owner needs operational control before commerce — order visibility and a place to manage everything.

- `/login` + `SignInForm` with a sign-in/create-account toggle (better-auth/react).
- `getCurrentAuth` claim-by-email: links the seeded admin profile to the owner's user on sign-in (`shadow@keebforge.in`, was `owner@…`).
- `AdminShell` (collapsible sidebar, mobile drawer, active-route highlight, sign-out) + `/admin/layout.tsx`; `ShowOnSite` client slot hides the site header/footer on `/admin`.
- Dashboard `/admin`: 8 metrics, revenue CSS bar chart (7/30/90/365 via `?range=`), repair pipeline, low stock, recent orders, recent activity, top products.
- Orders list `/admin/orders` (filters/sort/pagination) + order detail `/admin/orders/[orderNumber]` with `OrderDetailClient` (status, timeline, shipping, notes, archive) — server actions in `src/app/admin/actions/orders.ts`.
- `OrderAddress` model (`20260820000000_order_address`) snapshotting billing/shipping on orders.
- `src/lib/tracking.ts` `syncTrackingCache` — PII-free `Tracking` upsert called on every order mutation.
- `/admin/[...slug]` stub for unbuilt admin sections.
- Gotchas found: Next 16 `params`/`searchParams` are Promises → must be awaited (was `Cannot read properties of undefined (reading 'join')` in AdminStub); stale/multiple dev servers cause "Failed to fetch".
- Verified: unauthenticated `/admin*` → 307 `/login`; authed pages 200; full build green.

## Phase 9 — Product/shop management system (Part 1+2): catalog CRUD + inventory (2026-08-20)
**Why:** the shop is DB-driven, so the owner needs a full management UI before real inventory goes live (Phase 3+4 of the product system cover the customer-facing side).

- Migration `20260821000000_product_system`: `ProductStatus` enum; product fields (`status`, `costPrice`, `barcode`, `gstRate`, `weight`, dimensions, shipping, SEO, `popular`/`isNew`, ratings); `Category` nesting (`parentId`, `image`); `ProductVariant` `barcode`/`weight`; `ProductImage.variantId`; `InventoryMovement` ledger (+`variantId`, `Profile` inverse); `OrderItem` `discount`/`tax`.
- `src/lib/admin-catalog.ts` — admin queries (`getAdminProducts` with filters/sort/pagination + sales, `getAdminProduct`, categories/brands, inventory rows + movements, product stats, top products). `PRODUCT_TYPE_LABELS`/`PRODUCT_STATUS_LABELS` moved to client-safe `src/lib/product-labels.ts` (D-016).
- `src/app/admin/actions/catalog.ts` — zod-validated server actions: `saveProduct` (upsert, slugify, image-list sync), `setProductStatus`, `duplicateProduct`, `saveCategory`, `saveBrand`, `saveVariant`, `deleteVariant` (soft-disable if in cart/order), `adjustInventory` (receive/add/remove/adjust/damaged/lost in `$transaction` with atomic row read + increment), `importProducts` (CSV).
- Gated Cloudinary: `src/lib/cloudinary.ts` + `src/app/api/admin/upload/route.ts` — returns 501 until `CLOUDINARY_*` creds are set.
- Pages: `/admin/products` (list w/ filters, stock badges, sales), `/admin/products/new`, `/admin/products/[id]`, `/admin/products/[id]/edit` (9-section `ProductForm`), `/admin/products/categories` (nested), `/admin/products/inventory` (rows + inline adjust + movement ledger), `/admin/brands`, `/admin/products/import`, `/admin/products/export` (CSV route). Shared `ActionForm` (toast + spinner) and `ProductDetailClient` (`ProductStatusBar`, `VariantsManager`, `InventoryForm`).
- Seed now includes 8 brands + 11 demo products (D-014).
- Verified: typecheck/lint/build green; all admin routes 200 authed + 307 unauthed; shop lists seeded products; product detail renders all sections; export CSV complete; out-of-stock product hidden from shop (404).

## Phase "Auth hardening" — social login + deployment diagnosis (2026-08-20)
**Why:** customer accounts needed OAuth, and the Better Auth Dash onboarding could not connect to the deployed site.

- `better-auth.ts`: added `socialProviders` (google + discord, active only when env is set) and `trustedOrigins`.
- New endpoints: `/api/auth/me` (returns user + role; 401 signed-out) and `/auth/callback` (admin→`/admin`, else→`/`).
- Rewrote `SignInForm` (social buttons + email sign-in/sign-up toggle, role-based redirect) and `/login` as customer-facing; "Sign in" nav link (desktop + mobile, hidden when signed in); `.btn-social`/`.or-divider` CSS.
- `.env` / `.env.example`: `GOOGLE_CLIENT_ID/SECRET`, `DISCORD_CLIENT_ID/SECRET`, `BETTER_AUTH_API_KEY`.
- **Dash deployment diagnosis:** `https://keebforge.in/api/auth` 308→www is a **Vercel domain-level redirect**, not app code. Repo verified clean: `src/proxy.ts` excludes `api/`, no `vercel.json`/middleware, `next.config.ts` redirects are static slug maps. Deployed build is current (`www/api/auth/dash/validate` → 401). `.env` `BETTER_AUTH_URL` reverted to dev-localhost; prod apex values documented in `.env.example`.
- Verified locally: `/api/auth/me` 401, `/api/auth/dash/validate` 401 (plugin live), email sign-in against live DB.
### IA restructure: Shop / Mods / Workshop / Work (2026-08-24)

- Nav/footer/pages renamed to Shop | Mods | Workshop | Work per owner spec. URLs UNCHANGED by design: `/services` displays as "Mods", `/repair` as "Workshop", `/work` = portfolio. No redirects needed; old bookmarks still work.
- Shop categories relabeled via `SECTION_LABELS` in `src/lib/shop.ts` (DB product types CUSTOM/NEW/CLEARANCE untouched): Made to Order / Brand New / Clearance + spec taglines. Same labels feed category-page H1s and the navbar shop dropdown (`SHOP_LINKS` in SiteHeader).
- Service catalog groups/services come from `prisma.serviceGroup` (DB) — NOT renamed in code; group titles like "Switch Services" live in DB rows and are managed via admin. Mods page hero copy updated only.
- Remaining intentional "Repair"/"Services" strings: admin sidebar (/admin/services manages DB service records), order-type badge REPAIR (enum), WhyForge "Start a Repair →" verb CTA to /repair.
- Verified with Playwright: desktop nav, shop dropdown items, active state on all 4 sections (+/shop/clearance→Shop), mobile menu order, footer links, H1s on all five pages.
- Seed + admin renamed to Mods terminology (2026-08-24): serviceGroup display names in prisma/seed.ts → Switch Mods / Stabilizer Mods / Soldering & Switch Work / Custom PCB & Keyboard Work / Mouse Switch Mods ("Mouse Mods & Repairs" kept). SLUGS UNCHANGED (they are the pointers: HIDDEN_GROUP_SLUGS, checkout config, admin all reference them); seed upserts by slug so re-seeding renames in place. Admin sidebar: /admin/repairs → "Workshop", /admin/services → "Mods". WhyForge ghost CTA → "Explore Workshop →".
- URL rename (2026-08-24, owner request): `/services` → `/mods`, `/repair` → `/workshop` (dirs git-mv'd under (site)). All internal links updated (SiteHeader, Footer, Hero, WhyForge, CtaSection, ShopCatalog, cart, not-found, CheckoutClient ×4, ServiceSection SERVICE_HREF). next.config 307s old→new. Sitemap auto-updates. Configurator panel heading "Available … Mods". Admin routes /admin/services & /admin/repairs intentionally unchanged (DB-entity screens).
- Admin login fix (2026-08-24): two blockers found. (1) Owner password lost in earlier truncation → reset via `ctx.password.hash()` from `auth.$context` (better-auth.ts), temp pw given to owner. (2) Live org row had slug "admin" but `KEEBFORGE_ORG_SLUG = "keebforge"` guard expects "keebforge" → member lookup missed → /unauthorized; renamed org row to KeebForge/keebforge. Verified: sign-in 200, /admin + /admin/orders 200 with session.
- Buy Now → /checkout (2026-08-24): both product buy components (ProductConfigurator + AddToCart) had buyNow state routing to /cart; changed to /checkout. Item is added to cart by the same server action first, so checkout shows it. Playwright-verified with Keychron (configured) + Gateron (simple).
### Mods shipping/pickup flow (2026-08-24)

- `/mods` now 4 steps: Device Details → Mods → **Shipping/Pickup** (3-way segmented pill selector: customer_shipping / pickup / undecided) → **Package Details** (packed L/W/H cm ≤152 + weight kg ≤30) with live Order Preview showing Service Subtotal / Return+Pickup shipping lines / Estimated Total.
- Quotes reuse lib/shipping Delhivery integration server-side via new `POST /api/shipping/service-quote` ({pincode,lengthCm,widthCm,heightCm,weightKg} → {returnPaise,pickupPaise,totalPaise}). `calculateShipping`/`calculateShippingParams` gained optional `originPincode` override so the pickup leg (customer→workshop) can be quoted; cache key already covered both pins. Volumetric rule applies (45×35×12 @2.5kg → chargeable 3780g).
- Client states: idle/invalid-pin/missing-fields/calculating/ok/fail (500ms debounce); Add-to-Cart blocked until a successful quote when method ≠ undecided; undecided = "Shipping method will be confirmed before your order is processed.", no quote required, dims optional.
- Shipping block persisted on CartServiceItem.config JSON (`shipping: {method,address{street,city,state,pincode},package{…},quote{…}}`) — inputs stored so server can recalc at order time; amounts informational. Zod schema extended in /api/cart/service (optional → old stashes load). StoredServiceCheckout extended; sessionStorage round-trip restores method/address/dims.
- services/create-order untouched (still charges service subtotal only; shipping settled after inspection as before).
- Mods shipping fixes (2026-08-24, post-review): (1) shipping status messages were gated on `selectedCount>0` — choosing a method before picking services showed nothing; shipValidation note now always renders and empty-preview branch shows the status line. Message priority: invalid-pin → specific msg; missing pin/dims → "Enter a valid PIN code and package details to calculate shipping."; address fields missing → "Enter your full address to continue." (2) Estimated Total was adding quote.totalPaise (both legs) for customer_shipping — now method-aware: pickup→totalPaise, customer_shipping→returnPaise only. Verified DOM-scoped: subtotal 780 + return 329 = 1,109 ✓.
- Mods shipping v2 hardening (2026-08-24, spec-driven): manual "Calculate Shipping" button replaces debounced auto-quote (no Delhivery calls on keystrokes); quote staleness via shipInputKey snapshot — ANY change to method/PIN/package/service count hides amounts, shows "recalculate" note, blocks Add to Cart until re-quoted. New src/lib/package-limits.ts (MAX_DIM_CM=152, MAX_WEIGHT_KG=30) shared by client hints + service-quote route + cart route. Spec helper texts for all 3 options. Preview rows: pickup → Pickup/Return/Total shipping; customer_ships → Return/Total; undecided → "—" + "Shipping calculated after confirmation." PINCODE_UNAVAILABLE → "Shipping is currently unavailable for this PIN code." /api/cart/service now RECALCULATES both legs server-side before storage — client amounts never trusted (verified: tampered returnPaise=1 stored as real 32865). `totals` snapshot persisted but has zero consumers (display-only). Playwright matrix A–G + staleness + tamper + mobile-390 all green.
- Surface/Express delivery-speed selector (2026-08-24): mods shipping step gains "Delivery Speed" pills (rendered only when DELHIVERY_MODES offers >1; labels carry estimatedDaysFor day estimates). Page passes enabledShippingModes()+DEFAULT_SHIPPING_MODE as props (env-driven, no client env reads). service-quote + cart/service routes accept `mode`, validate against enabledShippingModes(), quote both legs in that mode. Stored in shipping.mode (zod optional for old rows). Mode is part of shipInputKey → switching invalidates the quote like any other input. Verified: surface ₹211.21 vs express ₹328.65 return live, stale-on-switch, server recalc uses stored mode. Note: amt() rounds per-row so row-sum can differ from paise-exact total by ≤₹1 — display convention, totals computed in paise.
- Step 05 — Your Details (2026-08-24): new panel after Package Details with Full Name / WhatsApp-Phone / Email / optional Additional contact info. Client validation (name ≥2, phone /^\+?\d{10,13}$/ after stripping spaces/dashes, email regex) gates Add to Cart via contactValidation; inline per-field messages. Persisted as `contact` on the cart service config (zod: name min2, phone regex, email, alt ≤280; optional for legacy rows) and in the sessionStorage stash with restore. Playwright: gating, inline errors, POST 200 + stored JSON, round-trip all green.
- Mods flow reorder (2026-08-24): Step 03 is now "Your Details"; Shipping/Pickup + Package Details merged into one "Step 04 — Shipping & Packaging" panel (Package Details as h3 sub-heading). No logic changes — all gating/validation/payload identical.
- Mods State dropdown (2026-08-24): Step 04 address State is now a <select> like workshop 06, styled via new `.config-page .field-inline select` chevron CSS. Extracted canonical INDIAN_STATES (36 entries, from CheckoutClient's fuller list) into src/lib/indian-states.ts; RepairIntake + CheckoutClient now import it (removed their divergent local copies). Verified: select renders 37 options, full add-to-cart flow with dropdown state persists ("Karnataka"), workshop page unaffected. NOTE: a mid-edit git restore mishap briefly emptied RepairIntake/CheckoutClient — both rebuilt from HEAD + re-applied edits; content verified by typecheck/build/e2e.
- Direct-pay mods booking (2026-08-24): /mods "Pay & Confirm" replaces Add-to-Cart — stashes full config (incl. contact) and routes straight to /checkout; NO cart write for mods anymore (/api/cart/service kept for legacy rows). Checkout boot() now falls back to the sessionStorage stash when cart has no service item; ServiceCheckout prefills name/email/phone/address/pin/state from stash contact+shipping and forwards contactNote + modsShipping{method,mode,address.pincode,package} to /api/services/create-order. Route extended: optional modsShipping+contactNote zod; BOTH legs re-quoted server-side, shipPaise added to totals.total/shipping, persisted on Order (shippingMode/WeightGrams/Origin/DestinationPincode columns + summary.modsShipping + summary.contactNote). Checkout display fix: Shipping row + "Pay ₹X with Razorpay" now include the mods shipping (were silently excluding it while server charged it). Verified: prefill ✓ Pay ₹991.21 (=780+211.21 surface) ✓ server charge 99121paise ✓ orderNumber issued ✓; quote-only path untouched.
- Stale-cart hijack fix (2026-08-24): legacy add-to-cart service rows (pre-direct-pay) were overriding the fresh Pay&Confirm stash at /checkout — boot() preferred cart, so users saw old quotes (e.g. ₹186.32) instead of the just-calculated ones. Fix: stash now wins over cart in boot(); payAndConfirm() also DELETEs any cart service item on the way out. Verified with seeded poisoned cart row.
- Pickup = 1.5× forward estimate (2026-08-24): no reverse-pickup rate exists in the Delhivery API, so /mods quotes ONE forward leg (origin → customer PIN) via /api/shipping/service-quote which now returns {forwardPaise} only; legs derive via shared isomorphic src/lib/shipping-estimate.ts (deriveLegs: customer_shipping → {0, fwd}; pickup → {ceil-to-whole-rupee(1.5×fwd), fwd}). Method removed from staleness key → Order Preview re-derives instantly on I'll ship/Need pickup switch with no recalc click; 1.5× explanation note shown when pickup selected; create-order + legacy cart route use the same deriveLegs so charge == display (verified: surface fwd ₹211.21 → pickup ₹317/₹211.21/₹528.21, server charged 130821p exactly).
- Address book upgrades (2026-08-24): Address model + migration gained name/email columns; /account/addresses form adds required Full Name + Email fields, phone now digits-only clamped to exactly 10 (client strip+slice, server ^\d{10}$ guard both POST & PATCH); address cards show recipient name + email; checkout saved-address prefill maps name/email too (DeliveryForm splits name into first/last).
- Saved addresses in mods flow (2026-08-24): logged-in customers get a "Use a saved address" dropdown atop Step 04's address inputs (fetches /api/account/addresses; 401 guests see nothing); selecting fills street/city/state/PIN plus contact name/email/phone (existing values win), then normal recalc gate applies since PIN changed.
- Landmark on saved addresses (2026-08-24): /account/addresses form gained "Landmark (Optional)" bound to the existing Address.apartment column (API accepted it all along); card renders it; checkout's SavedAddress interfaces now include apartment so both prefill paths (service form addressLine2 + delivery form apartment) carry the saved landmark.
- Workshop-style saved-address cards in mods Step 04 (2026-08-24): replaced "Use a saved address" dropdown with ri-address radio-card list ([...savedAddresses, null] pattern from RepairIntake); default address preselected & auto-fills addr+contact unless a stash restore already provided one; manual fields render only when no saved addresses or "Use a different address" picked.
- Checkout redesign (2026-08-24): ServiceCheckout rebuilt as compact 2-col layout — left: numbered Customer Details (3-across grid) + Shipping Address cards, secondary text-style Edit configuration link; right: sticky Order Summary ("Review & Pay" h1) with Your Configuration block (device icon/brand/layout/switch+stab quantities/keycaps), services with qty×rate lines, pickup → Pickup/Return/Total Shipping rows vs customer-ships → Return only (display-only from stash quote), lime 1.6rem total, CTA "Pay ₹X →" + "Secure payment · Razorpay". New globals.css .checkout-summary (sticky top 6rem @lg) because .card's position:relative was overriding Tailwind sticky utilities — product checkout aside uses it too. Lint fixes: de-memoized counts/activeServices/config/preview/validation in ServiceConfigurator (React Compiler preserve-manual-memoization errors), moved mods-prefill into ServiceCheckout useState initializer (set-state-in-effect). Zero logic/API changes; Razorpay modal verified opening.
- Shared AddressPicker (2026-08-24): extracted /mods radio-card address selector into src/components/services/AddressPicker.tsx (exports SavedAddressOption + <AddressPicker addresses selectedId onSelect>); SavedAddressOption moved out of ServiceConfigurator (apartment added as optional). /mods Step 04 and /checkout service section both render the identical card list ("Label · Default" + address line, plus "Use a different address"); manual fields render only when selectedId === "" or no saved addresses (guests). Checkout: dropdown removed; new selectedAddrId state; prefill selects the card matching the stashed mods address (street+PIN), else "" so stash stays visible; otherwise default card auto-applies. Same matching/fallback logic added to /mods loader. Verified: default preselect, toggle reveal, card→form apply, mismatch→manual, guest path, Razorpay opens.

## Razorpay EMI² Affordability Widget on /checkout (2026-08-25)

Official Native Web integration (per razorpay.com EMI² widget docs) embedded inside BOTH order summaries (ServiceCheckout + ProductCheckout), directly above the existing Pay button which is untouched:

- `src/components/payments/AffordabilityWidget.tsx` (new, client): loads `https://cdn.razorpay.com/widgets/affordability/affordability.js` exactly once via a module-level singleton promise (retryable on failure), renders into the docs-mandated `#razorpay-affordability-widget` element via `new window.RazorpayAffordabilitySuite({key, amount, currency:"INR", theme:{color:"#d4f700"}, display:{widget:{main:{isDarkMode:true}}}}).render()`. No documented update()/destroy() API exists → amount changes re-instantiate (replaceChildren first); deps `[amountPaise, keyId]` make it react to shipping/config changes. States: skeleton while loading, hidden entirely when no payable amount / missing key / script failure / render throw; failures log via console.warn in dev only. Payment button works regardless of widget state.
- Key ID plumbing: `/checkout` page.tsx (server component) passes `NEXT_PUBLIC_RAZORPAY_KEY_ID ?? RAZORPAY_KEY_ID ?? null` as a prop → CheckoutClient → widget. Same public value the create-order APIs already send to browsers; secret stays server-side (verified: 0 occurrences of RAZORPAY_KEY_SECRET in .next/static chunks).
- Types for the suite config added to `src/types/razorpay.d.ts` (documented surface only).
- Widget hides itself when total is null/0: product checkout before shipping resolves, service checkout in quote-only mode.
- NOT wired (on-demand Razorpay features, would be fake until enabled): checkout_callback (Buy-Now auto-select into Standard Checkout) and eligibility check — see roadmap for enablement steps.
- Verified with headless Chromium e2e against prod build: mods-flow checkout renders "Review & Pay" + Pay ₹950 → button intact; widget container visible with official `cdn.razorpay.com/widgets/affordability/frame.html` iframe rendering inside (177px tall); exactly 1 affordability script tag; zero hydration errors; mobile 390px shows widget; guest 401 on /api/auth/me is the only console error (pre-existing). Product-checkout path verified hidden-by-design locally (seeded demo products lack shipping config → Delhivery 422 MISSING_SHIPPING_CONFIGURATION → total null → widget hidden gracefully; will mount once real shipping-configured products exist). typecheck/lint/build green.
- Blank-widget root cause FIXED (2026-08-25): the skeleton phase held `#razorpay-affordability-widget` at `display:none` while `render()` ran — Razorpay measures container width during render → sized its iframe 0px wide → permanently blank. Fix: container is never display:none (always measurable); the loading skeleton became an absolute overlay with min-height on the wrapper. Post-fix e2e: iframe renders 336×111 with real content (pixel stdev ~27/channel, not blank), affordability API `GET /v1/checkout/affordability?key_id=rzp_test_…&amount=<dynamic>` returns **200 `enabled:true`** with EMI/Cardless-EMI/PayLater/Offer entities for both ₹95000 and ₹567000 paise test totals (amount provably dynamic; currency INR via config). Dashboard's "Payment Offers"/"Affordable Payment Options" Enable Now toggles control how many of those entities surface, but data already flows.
- Repair intake review-submit FIXED (2026-08-25): "Invalid input: expected string, received null" on Submit Request was pre-existing — review phase unmounts step inputs; `description`, `phone`, `email`, `firstName`, `lastName` had no hidden mirror inputs, so the server action read nulls. Added all five mirrors. e2e: guest flow → Submit returns `{ok:true, orderNumber:"KFT5YNZD"}` with joined customerName in DB.
- Shipping Address loading flash FIXED (2026-08-25): service checkout rendered the manual address form until the saved-address fetch resolved, then swapped to the picker — a visible layout jump. Root cause: `addresses` state starts `[]`, and render treated "empty" as "no saved addresses" while the request was pending. Added `addressesLoading` (default true), flipped via `prefill().finally()` so every exit path ends it (guest 401, empty list, error, early return); render now gates on it: skeleton → AddressPicker (if saved) → manual form (only when confirmed empty). Also defined the `.skeleton` class that was referenced by checkout's Suspense fallback but never existed in any CSS (dark shimmer from --bg2/--bg3 + skeleton-wave keyframes). Default-address selection + form prefill stay batched in one commit (no extra render cycle). Probe /tmp/opencode/pwtest/probe-addr-loading.js: 13/13 PASS with mocked auth + 1.5s-throttled addresses API — skeleton during fetch, zero manual inputs/picker mid-load for saved users, title y stable 463→463, default auto-selected, guest sees brief skeleton then manual form; layout probe 17/17 and billing probe all-PASS (footer checks updated to expect absence per user request). via new `.checkout-page { --max-w: 1280px }` scope on all checkout `<main>`s (site default stays 1160); grid `minmax(0,1fr)_360px` gap-6; "← Edit configuration" removed from 01 header ("← Back to Configuration" kept); policy footer removed from checkout per user; cards tightened to `17px 20px` via `.checkout-page .card` (unlayered `.card` beats Tailwind utilities — arbitrary p-5 had silently never applied); Order Summary qty format flipped to `×65` / `×4`. CRITICAL site-wide fix: body `overflow-x: hidden` forced computed overflow-y:auto → body was a scroll container that never scrolls, silently breaking ALL position:sticky site-wide (incl. the pre-existing .checkout-summary sticky). Changed to `overflow-x: clip` (same visuals, no scroll container) — sticky now genuinely pins at 6rem mid-form. Layout probe (/tmp/opencode/pwtest/probe-layout.js): 16/16 PASS (1280px centered, heading aligns to grid pad-left, gaps 15px, widget 316×177 above Pay ₹470, mobile single-col no sticky no overflow). Note: root font-size is 15px on this site (gap-6=22.5px), and probe baselines must be taken after the async Razorpay iframe settles.
- Checkout Payment + Billing sections added (2026-08-25, Parix-reference redesign): service flow left column is now 01 Customer Details → 02 Shipping Address → 03 Payment → 04 Billing Address. Payment card = Razorpay gateway (UPI/VISA/MC chips + "+more", "redirected to Razorpay's secure checkout" copy — gateway represented honestly, never Cashfree), lime-accent selected state. Billing = "Same as shipping" default / "Use a different billing address" revealing Full Name, Addr1/2, City, State select (INDIAN_STATES), PIN (6-digit), Phone with the same Field/fieldClass/shop-select styles + validation rules; `billingSame` + `billing` client state gates formValid and handlePay; payload sends `billingSameAsShipping` + optional `billingAddress` (server zod non-strict strips for now — ponytail: persist when Order gets billing columns). Checkout footer links row (Refund→/terms, Shipping→/faq, Privacy→new /privacy-policy placeholder page in terms-page pattern, Terms→/terms, Contact→/contact) below the grid, wraps on mobile. React Compiler rejected useMemo(validateBilling) → plain call. e2e vs prod build: 28/28 PASS incl. widget 336×111 above Pay ₹470, empty/invalid-PIN disables Pay, valid re-enables, order KF256X3J via UI + direct different-billing POST KFYAH3P4 both 200, mobile no overflow.
- Full Name → First/Last split completed everywhere (2026-08-25): service checkout + mods configurator Step 03 + repair intake + address book forms now show separate First/Last Name inputs (product checkout, profile page, payments API were already split). Storage stays single-string by design: Better Auth User.name untouched, Profile/Address.name and Order.customerName receive joined "First Last" from the APIs (`services/create-order` customer zod now firstName/lastName; repair action likewise; addresses POST/PATCH join). Configurator stash `ModsContactInput` renamed name→firstName/lastName with legacy-stash compat on restore; saved-address prefill splits stored `name`. Service checkout Customer Details rows: [First|Last] then [Phone|Email]. e2e vs prod build: labels split, configurator carry-over prefills both inputs, empty last name disables Pay button, Grace Hopper order KFC8CV7N created with `customerName:"Grace Hopper"` and official Razorpay Checkout modal opened. typecheck/lint/build green.
- Full e2e verification pass (2026-08-25, later): removed `overflow-hidden` from the widget wrapper (req: no overflow clipping — Razorpay's expanded layers must never clip). Verified against prod build with REAL service rows: widget API amounts track payable totals exactly (₹32,000 / ₹62,000 paise fixtures; ₹95,000 / ₹5,67,000 earlier); "View plans"/"View offers" launchers each open Razorpay's own `rzpAffordabilityDetailFrame` modal (launcher copy confirmed live: "EMI from ₹978/month", "Save up to ₹500"); Pay button → `/api/services/create-order` 200 (real `order_TTq…`, server re-quotes shipping, INR) → official Standard Checkout `razorpay-checkout-frame` (api.razorpay.com) opens with all payment blocks. Affordability API returns `enabled:true` with ALL category families (emi, cardless_emi, paylater, offers, other_emi, card, netbanking, upi, wallet) for this test key. Only console noise: pre-existing guest /api/auth/me 401 + Razorpay-internal data:-iframe sessionStorage pageerrors + its `build/undefined` ORB fetch — zero app-side errors, single script tag/init always.
